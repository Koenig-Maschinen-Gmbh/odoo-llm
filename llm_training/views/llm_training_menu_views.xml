<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Training → Configuration → Training & Generation (was top-level) -->
    <menuitem
        id="menu_llm_training_root"
        name="Training"
        parent="llm.menu_llm_training_generation"
        sequence="10"
    />

    <menuitem
        id="menu_llm_training_dataset"
        name="Datasets"
        action="llm_training_dataset_action"
        parent="menu_llm_training_root"
        sequence="10"
    />

    <menuitem
        id="menu_llm_training_job"
        name="Jobs"
        action="llm_training_job_action"
        parent="menu_llm_training_root"
        sequence="20"
    />
</odoo>
