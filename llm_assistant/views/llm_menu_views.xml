<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!-- Assistants → Configuration -->
    <menuitem
        id="menu_llm_assistant"
        name="Assistants"
        parent="llm.menu_llm_config"
        action="action_llm_assistant"
        sequence="25"
    />

    <!-- Prompt Templates → Configuration (was "Prompts" at root level) -->
    <menuitem
        id="llm_prompt_menu"
        name="Prompt Templates"
        parent="llm.menu_llm_config"
        action="llm_prompt_action"
        sequence="35"
    />

    <!-- Prompt Templates sub-menus (Categories, Tags) -->
    <menuitem
        id="llm_prompt_configuration_menu"
        name="Prompt Configuration"
        parent="llm.menu_llm_config"
        sequence="36"
    />

    <menuitem
        id="llm_prompt_category_menu"
        name="Categories"
        parent="llm_prompt_configuration_menu"
        action="llm_prompt_category_action"
        sequence="20"
    />

    <menuitem
        id="llm_prompt_tag_menu"
        name="Tags"
        parent="llm_prompt_configuration_menu"
        action="llm_prompt_tag_action"
        sequence="30"
    />
</odoo>
