<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Generation → Configuration → Training & Generation (was top-level) -->
    <menuitem
        id="menu_llm_generation_root"
        name="Generation"
        parent="llm.menu_llm_training_generation"
        sequence="30"
    />

    <menuitem
        id="menu_llm_generation_job"
        name="Jobs"
        parent="menu_llm_generation_root"
        action="action_llm_generation_job"
        sequence="10"
    />

    <menuitem
        id="menu_llm_generation_queue"
        name="Queues"
        parent="menu_llm_generation_root"
        action="action_llm_generation_queue"
        sequence="20"
    />
</odoo>
